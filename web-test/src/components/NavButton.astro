---
export interface Props extends astroHTML.JSX.AnchorHTMLAttributes {}

const { href, class: classValues, ...props } = Astro.props;

const { pathname } = Astro.url;
const isCurrent = href === pathname || href === pathname.replace(/\/$/, "");
---

<li class="mr-2 flex-1">
  <a
    class:list={[
      "block rounded border px-4 py-2 text-center",
      {
        "border-white text-neutral-500 hover:border-neutral-200 hover:bg-neutral-200":
          !isCurrent,
      },
      {
        "border-neutral-500 bg-neutral-500 text-white hover:bg-neutral-700":
          isCurrent,
      },
    ]}
    {href}><slot /></a
  >
</li>
